#include"main.h"
/**
 * add - sum of two numbers.
 * @n: Check the input
 * @x: Check the input
 * Return: the value of the sum
 */
int add(int n, int x)
{
	int somme;

	somme = n + x;
	return (somme);
}
